# MapTiler API Key — free tier at https://maptiler.com (100K requests/month)
# Sign up, create a key, and paste it here.
# Without a key the map falls back to CARTO Dark Matter (functional but no terrain).
VITE_MAPTILER_KEY=
