/**
 * Re-exports the unified StatCard from the UI kit.
 * Deck slides and platform panels share the same component.
 */
export { StatCard } from '../ui/StatCard'
export type { StatCardProps } from '../ui/StatCard'
